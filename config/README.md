# Configuration

`minimal.example.json` exercises the immutable offline runner against a caller-supplied data manifest. Full raw-data runs add `lookback_days`, `walk_forward`, model, portfolio, execution and performance sections described by the ticket spec. Keep local overrides and every secret untracked; the evidence writer rejects secret-like config fields.
